import React from "react";

const Header = () => {
    return (
        <header className="text-center mb-4">
            <h1 className="display-4 text-primary">Cargo Ship Berth Planner</h1>
            <p className="lead text-muted">Manage and edit berth assignments for incoming cargo ships</p>
        </header>
    );
};

export default Header;
